function CountryRecord({ flag, name, population, area, region, id , unMember, independent }) {
  return {
    id,
    flag,
    name,
    population,
    area,
    region,
    unMember,
    independent,
  }
}

export default CountryRecord;
