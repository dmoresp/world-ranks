function CountryDetail({
  flag,
  name,
  officialName,
  population,
  area,
  capital,
  subregion,
  languages,
  currencies,
  continents,
  neighbours,
  id,
}) {
  return {
    flag,
    name,
    officialName,
    population,
    area,
    capital,
    subregion,
    languages,
    currencies,
    continents,
    neighbours,
    id,
  };
}

export default CountryDetail;
